# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# PyTorch weights and states
*.pt
*.pth
*.ckpt

# Logs
*.log

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Project specific
trained_hfp_baby/
hfp_hf_test_save/
benchmark_*.txt
hfp_upload_temp/
error.log
hfp_hf_test_save/

# Generated experiment outputs (never commit)
# HFP_CKPT_DIR default; run artifacts
checkpoints/
# sweep/dense/baseline/pure-memory result logs
review_scripts/*_results.txt
sweep_results.txt

# Internal / legacy — not for the public repo
# internal review-agent prompt/brief (local paths)
FABLE_GOREV_PROMPT.md
INCELEME_BRIFINGI.md
# orphaned old model kept only for local diffing
_legacy_reference/
# dataset — fetch separately, don't commit
tinyshakespeare.txt
osf_companion_v22.tex          # transient build copy (see osf_companion.tex)

# NOTE: review_scripts/ is now TRACKED (was fully ignored). The reproduction
# scripts (verify_claims.py, dense_retention.py, length_gen.py,
# interference_eval.py, baseline_compare.py, pure_memory_ablation.py,
# run_remaining_experiments.sh) are cited publicly by RESULTS.md and
# osf_companion.tex, so they belong in the repo. Only their generated outputs
# (above) stay ignored.

# --- reroot temizligi: yayinlanmaz ---
_archive_old/
_legacy_reference/
hf_upload/
*.safetensors
osf_companion_v22.tex
COMMIT_STEPS.md
DUZELTME_PLANI.md
REPO_STRUCTURE.md
OSF_GUNCELLEME_NOTU.md
YAYINLAMA_TALIMATI.md
DEGISIKLIKLER.md

# Session artifacts
_commit_msg.txt

# Local archive (not for the public repo)
_arsiv/
